-@articles.each do |article|

  %h1#article-title
    =link_to article.show_title, "/article/#{article.title}"

  #article
    =article.content

  %p#time-stamp
    建立时间: #{article.created}<br/>更新时间: #{article.updated}

  %hr
  %br
  %br

#paginate
  =link_to "更多", "?starts=#{@articles[-1].id}"
